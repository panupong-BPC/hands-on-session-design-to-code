export type LoginFormValues = {
  userId: string;
  password: string;
};

export type LoginUser = {
  userId: string;
  fullName: string;
  role: string;
  branchCode: string;
  branchName: string;
};
